import { environment } from "./environment";

export const ROUTES = {
  base: environment.baseUrl,
  login: 'login',
  users: 'users',
  projects: 'projects',
  clients: 'clients',
  tasks: 'tasks',
  tasks_registers: 'tasks_registers'
}
